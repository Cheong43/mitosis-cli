interface:
  display_name: "Mitosis Cli"
  short_description: "Repo-specific patterns and workflows for mitosis-cli"
  default_prompt: "Use the mitosis-cli repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true